{
  "name": "backend",
  "scripts": {
    "dev": "wrangler dev --config wrangler.local.toml",
    "deploy": "wrangler deploy --config wrangler.local.toml --minify"
  },
  "dependencies": {
    "@aradhya042/common-medium": "^1.0.2",
    "@prisma/client": "^5.21.1",
    "@prisma/extension-accelerate": "^1.2.1",
    "hono": "^4.6.5",
    "prisma": "^5.21.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.79.0"
  }
}
